local epiShit = "images/episodeshits"

createImage(epiShit, "bg", addImage, 0,90)
addImage("bg", 0,90, setScrollFactor(19,100))

createImage(epiShit, "billyRobot", addImage, 130,560)
addImage("billyRobot", add)

createImage(epiShit, "episodeTitle", addImage, middle, Top)
addImage("episodeTItle", createImage)

createImage(epiShit, "mandy", addImage, 100,800)
addImage("bg", 0,90, setScrollFactor)

if StoryMode == then
{
    epiShit.visble = true
    epiShit.episodeDial

    episodeDial.play:(0.65,. 1)

    psychDefaultFade.fadeOut = true

    wait(episodeDial) then
    {
        startSong("data/billybot", songStartUp) = true
    }

    

    print("loaded up episode card")
}
